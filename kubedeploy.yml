apiVersion: apps/v1
kind: Deployment
metadata:
  name: pyapp
  labels:
    app: npapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: npapp
  template:
    metadata:
      labels:
        app: npapp 
    spec:
      containers:
      - name: myapp-cont
        image: nginx:latest
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: pyapp
spec:
  type: NodePort
  selector:
    app: npapp
  ports:
   - protocol: TCP
     port: 80
     targetPort: 80
     nodePort: 30008
